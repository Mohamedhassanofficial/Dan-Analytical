# --- Frontend dev environment ---
# The Vite dev server proxies /api/v1/* to http://localhost:8000 by default
# (see vite.config.ts). Override here if your backend runs elsewhere.

VITE_API_BASE_URL=/api/v1

# Root pointer — the canonical requirements file lives at backend/requirements.txt
# so Render's `rootDir: backend` build can install with a flat path. Running
# `pip install -r requirements.txt` from the repo root still works because pip's
# `-r` directive recursively includes the referenced file.
-r backend/requirements.txt
